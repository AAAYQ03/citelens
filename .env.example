# Server-side Anthropic key (optional — visitors can also bring their own key,
# and the built-in demo works with no key at all)
ANTHROPIC_API_KEY=
# Model used for grounded answers (must support the Citations API)
ANSWER_MODEL=claude-haiku-4-5-20251001
